apply plugin: "war"

bootWar {
    mainClass = "com.github.aawe44.CourseEnrollmentBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
